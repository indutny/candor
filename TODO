* ES-harmony-like varargs - a(x, y, z...) {}
  (and if have no significant runtime overhead a(x, y..., z) ).
* Lazy arguments
* Cons strings
* On-stack replacement and profile-based optimizations (register allocation too)
* Incremental GC
* Usage in multiple-threads (aka isolates)
* gdbjit
* Ast node ids
* Dtrace :)
