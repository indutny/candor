#include "scope.h"

namespace dotlang {

ScopeSlot* Scope::GetSlot(const char* name,
                          uint32_t length,
                          ScopeSlotType type) {
  return NULL;
}

} // namespace dotlang
